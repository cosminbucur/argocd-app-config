# argocd app configuration
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-server-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://gitlab.com/freeminions/argocd-app-config.git
    targetRevision: HEAD
    path: dev
  destination: 
    # local minikube
    server: https://kubernetes.default.svc
    namespace: gitops-server

  syncPolicy:
    syncOptions:
    # create the namespace if it doesn't exist
    - CreateNamespace=true

    automated:
      # auto sync with git repository 
      selfHeal: true
      prune: true